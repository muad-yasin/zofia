{
  "name": "@zofia/reader",
  "version": "0.2.0",
  "private": true,
  "description": "Zofia week-1 reader spike: session-state observation bridge (PLAN.md §2) — statusline/hook shim + zofia-reader CLI.",
  "type": "module",
  "bin": {
    "zofia-reader": "./bin/zofia-reader.mjs",
    "zofia-reader-install": "./install/install.mjs",
    "zofia-reader-uninstall": "./install/uninstall.mjs"
  },
  "scripts": {
    "test": "node --test"
  },
  "engines": {
    "node": ">=22"
  }
}
