{
  "$comment": "PLAN.md §5. Edit this file only, then run `npm run gen:providers`. v1 ships the owner's Claude Code subscription alone; other providers and local LLMs are out of scope until he decides otherwise (PLAN.md §9.2). Every UNKNOWN below is a PLACEHOLDER waiting on owner decision 2 (TODO.md), not a value.",
  "version": 1,
  "default_model": {
    "value": "sonnet",
    "source": "DECISIONS.md, owner decision 4 (2026-09-23): C&C runs Sonnet, latest build, via Claude Code's own `sonnet` alias"
  },
  "providers": [
    {
      "id": "anthropic",
      "label": "Claude Code (owner's subscription)",
      "launch": {
        "kind": "claude-code-cli",
        "effort_key": { "value": "UNKNOWN", "source": "UNKNOWN" }
      },
      "models": [
        {
          "id": "sonnet",
          "label": "Sonnet (latest)",
          "source": "Claude Code CLI `--model` alias; resolved by the CLI itself at launch",
          "effort_levels": "UNKNOWN"
        },
        {
          "id": "opus",
          "label": "Opus (latest)",
          "source": "Claude Code CLI `--model` alias; resolved by the CLI itself at launch",
          "effort_levels": "UNKNOWN"
        },
        {
          "id": "haiku",
          "label": "Haiku (latest)",
          "source": "Claude Code CLI `--model` alias; resolved by the CLI itself at launch",
          "effort_levels": "UNKNOWN"
        }
      ]
    }
  ],
  "aliases": {}
}
