{
  "$comment": "PLAN.md §5. Edit this file only, then run `npm run gen:providers`. v1 ships the owner's Claude Code subscription alone; other providers and local LLMs are out of scope until he decides otherwise (PLAN.md §9.2). Every UNKNOWN below is a PLACEHOLDER for a value not yet measured (docs/field-availability.md), not a value.",
  "version": 1,
  "default_model": {
    "value": "sonnet",
    "source": "DECISIONS.md, owner decision 4 (2026-09-23): C&C runs Sonnet, latest build, via Claude Code's own `sonnet` alias"
  },
  "default_effort": {
    "value": "medium",
    "source": "DECISIONS.md, owner decision 2 (2026-09-23): center seat's default effort is Medium. An owner choice, not a measured value; no launch path applies it until effort_key is measured"
  },
  "providers": [
    {
      "id": "anthropic",
      "label": "Claude Code (owner's subscription)",
      "launch": {
        "kind": "claude-code-cli",
        "effort_key": { "value": "UNKNOWN", "source": "UNKNOWN" }
      },
      "models": [
        {
          "id": "sonnet",
          "label": "Sonnet (latest)",
          "source": "Claude Code CLI `--model` alias; resolved by the CLI itself at launch",
          "effort_levels": "UNKNOWN"
        },
        {
          "id": "opus",
          "label": "Opus (latest)",
          "source": "Claude Code CLI `--model` alias; resolved by the CLI itself at launch",
          "effort_levels": "UNKNOWN"
        },
        {
          "id": "haiku",
          "label": "Haiku (latest)",
          "source": "Claude Code CLI `--model` alias; resolved by the CLI itself at launch",
          "effort_levels": "UNKNOWN"
        }
      ]
    }
  ],
  "aliases": {}
}
